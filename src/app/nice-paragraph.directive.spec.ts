import { NiceParagraphDirective } from './nice-paragraph.directive';

describe('NiceParagraphDirective', () => {
  it('should create an instance', () => {
    const directive = new NiceParagraphDirective();
    expect(directive).toBeTruthy();
  });
});
